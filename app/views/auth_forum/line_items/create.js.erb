var element = $('.cart-item-count');
if(element.length > 0) {
   element.text(<%= @cart.line_items.count %>);
}
else {
    $('.cart-icon-wrapper a').append("<span class='cart-item-count'> <%= @cart.line_items.count %> </span>");
}

