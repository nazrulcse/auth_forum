var element = $('.cart-item-count');
if(element.length > 0) {
   element.text(<%= @cart.total_cart_item %>);
}
else {
    $('.cart-icon-wrapper a').append("<span class='cart-item-count'> <%= @cart.total_cart_item %> </span>");
}

